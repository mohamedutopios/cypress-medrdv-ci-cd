# =============================================================================
#  MedRDV - Pipeline CI/CD
#  Jobs : install → build → cypress
# =============================================================================

name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  PHP_VERSION: "8.2"
  NODE_VERSION: "18"

jobs:

  # ===========================================================================
  #  JOB 1 : INSTALLATION
  # ===========================================================================
  install:
    name: 📦 Installation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout du code
        uses: actions/checkout@v4

      - name: Setup PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: pdo_mysql, intl, zip, opcache
          tools: composer:v2
          coverage: none

      - name: Composer install
        run: composer install --no-scripts --no-interaction --prefer-dist --optimize-autoloader

      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: npm install
        run: npm install

      - name: Upload vendor/
        uses: actions/upload-artifact@v4
        with:
          name: vendor
          path: vendor/
          retention-days: 1

      - name: Upload node_modules/
        uses: actions/upload-artifact@v4
        with:
          name: node_modules
          path: node_modules/
          retention-days: 1

  # ===========================================================================
  #  JOB 2 : BUILD & VALIDATION
  # ===========================================================================
  build:
    name: 🔨 Build & Validation
    runs-on: ubuntu-latest
    needs: install

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_pass
          MYSQL_DATABASE: medrdv_db
          MYSQL_USER: medrdv_user
          MYSQL_PASSWORD: medrdv_pass
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: "mysql://medrdv_user:medrdv_pass@127.0.0.1:3306/medrdv_db?serverVersion=8.0.32&charset=utf8mb4"
      APP_ENV: dev

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: pdo_mysql, intl, zip, opcache

      - name: Download vendor/
        uses: actions/download-artifact@v4
        with:
          name: vendor
          path: vendor/

      - name: Forcer bundles.php
        run: |
          cat > config/bundles.php << 'BUNDLES'
          <?php
          return [
              Symfony\Bundle\FrameworkBundle\FrameworkBundle::class => ['all' => true],
              Doctrine\Bundle\DoctrineBundle\DoctrineBundle::class => ['all' => true],
              Doctrine\Bundle\MigrationsBundle\DoctrineMigrationsBundle::class => ['all' => true],
              Symfony\Bundle\TwigBundle\TwigBundle::class => ['all' => true],
              Doctrine\Bundle\FixturesBundle\DoctrineFixturesBundle::class => ['dev' => true, 'test' => true],
          ];
          BUNDLES

      - name: Cache clear
        run: php bin/console cache:clear

      - name: Lint templates Twig
        run: php bin/console lint:twig templates/

      - name: Lint conteneur Symfony
        run: php bin/console lint:container

      - name: Valider schéma Doctrine
        run: |
          php bin/console doctrine:database:create --if-not-exists
          php bin/console doctrine:schema:update --force
          php bin/console doctrine:schema:validate

      - name: Charger les fixtures
        run: php bin/console doctrine:fixtures:load --no-interaction

  # ===========================================================================
  #  JOB 3 : TESTS CYPRESS + RAPPORT HTML MOCHAWESOME
  # ===========================================================================
  cypress:
    name: 🧪 Tests Cypress
    runs-on: ubuntu-latest
    needs: install

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_pass
          MYSQL_DATABASE: medrdv_db
          MYSQL_USER: medrdv_user
          MYSQL_PASSWORD: medrdv_pass
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h localhost"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    env:
      DATABASE_URL: "mysql://medrdv_user:medrdv_pass@127.0.0.1:3306/medrdv_db?serverVersion=8.0.32&charset=utf8mb4"
      APP_ENV: dev

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: pdo_mysql, intl, zip, opcache

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Download vendor/
        uses: actions/download-artifact@v4
        with:
          name: vendor
          path: vendor/

      - name: Download node_modules/
        uses: actions/download-artifact@v4
        with:
          name: node_modules
          path: node_modules/

      - name: Forcer bundles.php
        run: |
          cat > config/bundles.php << 'BUNDLES'
          <?php
          return [
              Symfony\Bundle\FrameworkBundle\FrameworkBundle::class => ['all' => true],
              Doctrine\Bundle\DoctrineBundle\DoctrineBundle::class => ['all' => true],
              Doctrine\Bundle\MigrationsBundle\DoctrineMigrationsBundle::class => ['all' => true],
              Symfony\Bundle\TwigBundle\TwigBundle::class => ['all' => true],
              Doctrine\Bundle\FixturesBundle\DoctrineFixturesBundle::class => ['dev' => true, 'test' => true],
          ];
          BUNDLES

      - name: Préparer la base de données
        run: |
          php bin/console cache:clear
          php bin/console doctrine:database:create --if-not-exists
          php bin/console doctrine:schema:update --force
          php bin/console doctrine:fixtures:load --no-interaction

      - name: Démarrer le serveur PHP
        run: |
          php -S 127.0.0.1:8000 -t public/ &
          sleep 3
          curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8000 || true

      - name: Lancer Cypress
        uses: cypress-io/github-action@v6
        with:
          install: false
          wait-on: "http://127.0.0.1:8000"
          wait-on-timeout: 30
          config: baseUrl=http://127.0.0.1:8000
          browser: chrome

      - name: Upload rapport Cypress HTML (mochawesome)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-report-html
          path: cypress/reports/
          retention-days: 30

      - name: Upload screenshots en cas d'échec
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-screenshots
          path: cypress/screenshots/
          retention-days: 7

      - name: Upload vidéos Cypress
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cypress-videos
          path: cypress/videos/
          retention-days: 7
          if-no-files-found: ignore